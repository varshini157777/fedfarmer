// Stub file: the real Auth context lives in useAuth.jsx
// Kept as a non-JSX module to avoid build-time JSX parsing errors.

export const useAuth = () => {
  throw new Error('useAuth.js stub should not be imported. Use useAuth.jsx instead.');
};

export const AuthProvider = ({ children }) => children;
